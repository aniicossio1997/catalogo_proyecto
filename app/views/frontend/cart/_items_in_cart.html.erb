<% @cart.each_with_index do |item, index| %>
<tr id='item-<%= index %>%'>
  <td><%= item.image %><%= item.name %></td>
  <td><%= item.price %></td>
  <td><%= item.count %></td>
  <td><%= item.count * item.price %></td>
</tr>
<% end %>